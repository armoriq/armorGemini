description = "Stage a policy rule change (verb + target + optional note). Preview only - /armor:yes to apply."

prompt = """The user ran /armor:add {{args}}. Below is the raw output from the ArmorGemini backend CLI. The output includes a YAML preview of the exact policy that will be sent to ArmorIQ. Report it verbatim - do not invent additional rules, do not confirm the change on your own. The user must run /armor:yes to apply the proposal, or /armor:no to discard it.

!{node __ARMORGEMINI_HOME__/scripts/armor/cli.mjs add {{args}}}
"""
