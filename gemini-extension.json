{
  "name": "armorgemini",
  "version": "0.3.0",
  "description": "Intent-based security enforcement for Gemini CLI. Every tool call is checked against a per-turn intent plan (must be declared via register_intent_plan) and your ArmorIQ policy before it runs.",
  "mcpServers": {
    "armorgemini-policy": {
      "command": "node",
      "args": ["${extensionPath}/scripts/policy-mcp.mjs"],
      "cwd": "${extensionPath}"
    }
  }
}
